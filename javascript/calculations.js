// Calculations -> Add, Subtract, Multiply, Divide

function add(x, y) {
    // Todo: Write a function to return the addition of x and y
    return x + y;
}

function subtract(x, y) {
    // Todo: Write a function to return the subtraction of x and y
    return x - y;
}

function multiply(x, y) {
    // Todo: Write a function to return the mulltiplication of x and y
    return x * y;
}

function divide(x, y) {
    // Todo: Write a function to return the division of x and y
    return x / y;
}

var x = 13;
var y = 4;

console.log(`Add: ${add(x, y)}`);
console.log(`Subtract: ${subtract(x, y)}`);
console.log(`Multiply: ${multiply(x, y)}`);
console.log(`Divide: ${divide(x, y)}`);